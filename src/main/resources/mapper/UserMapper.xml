<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.modules.userManage.dao.UserMapper">

    <select id="selectAll" resultType="org.example.modules.userManage.entity.User">
        SELECT id, username, password, email, role, create_time
        FROM user
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultType="org.example.modules.userManage.entity.User">
        SELECT id, username, password, email, role, create_time
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, role, create_time)
        VALUES (#{username}, #{password}, #{email}, #{role}, #{createTime})
    </insert>

    <update id="update">
        UPDATE user
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="email != null and email != ''">email = #{email},</if>
            <if test="role != null">role = #{role},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <delete id="removeByIds">
        DELETE FROM user
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectByUsername" resultType="org.example.modules.userManage.entity.User">
        SELECT id, username, password, email, role, create_time
        FROM user
        WHERE username = #{username}
            LIMIT 1
    </select>
</mapper>