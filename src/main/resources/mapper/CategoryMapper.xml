<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.modules.categoryManage.dao.CategoryMapper">

    <resultMap id="CategoryResultMap" type="org.example.modules.categoryManage.entity.Category">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="description" column="description" />
        <result property="level" column="level" />
        <result property="cateId" column="cate_id" />
        <result property="parentName" column="parent_name" />
    </resultMap>
    <select id="list" resultMap="CategoryResultMap">
        SELECT
        c1.*,
        c2.name AS parent_name
        FROM category c1
        LEFT JOIN category c2 ON c1.cate_id = c2.id
        <where>
            <if test="id != null and id != ''">AND c1.id = #{id}</if>
            <if test="name != null and name != ''">AND c1.name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="level != null and level != ''">AND c1.level = #{level}</if>
        </where>
        ORDER BY c1.level ASC, c1.id DESC
    </select>
    <select id="findAllWithParent" resultMap="CategoryResultMap">
        SELECT
        c1.*,
        c2.name AS parent_name
        FROM category c1
        LEFT JOIN category c2 ON c1.cate_id = c2.id
        <where>
            <if test="id != null and id != ''">AND c1.id = #{id}</if>
            <if test="name != null and name != ''">AND c1.name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="level != null and level != ''">AND c1.level = #{level}</if>
        </where>
        ORDER BY c1.level ASC, c1.id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countSearch" resultType="int">
        SELECT COUNT(*) FROM category
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="level != null and level != ''">AND level = #{level}</if>
        </where>
    </select>

    <insert id="insert" parameterType="org.example.modules.categoryManage.entity.Category"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, cate_id,level)
        VALUES (#{name}, #{description}, #{cateId},#{level})
    </insert>

    <update id="update" parameterType="org.example.modules.categoryManage.entity.Category">
        UPDATE category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="cateId != null">cate_id = #{cateId},</if>
            <if test="level != null">level = #{level},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <select id="countSubCategories" resultType="int">
        SELECT COUNT(*) FROM category WHERE cate_id = #{id}
    </select>

</mapper>