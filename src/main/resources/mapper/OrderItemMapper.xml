<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.modules.orderItemManage.dao.OrderItemMapper">

    <!-- 通用结果集映射 -->
    <resultMap id="OrderItemResultMap" type="org.example.modules.orderItemManage.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="good_id" property="goodId"/>
        <result column="good_name" property="goodName"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
    </resultMap>

    <!-- 根据订单ID查询明细 -->
    <select id="selectByOrderId" resultMap="OrderItemResultMap">
        SELECT id, order_id, good_id, good_name, price, quantity
        FROM order_item
        WHERE order_id = #{orderId}
    </select>

    <!-- 新增订单明细 -->
    <insert id="insert" parameterType="org.example.modules.orderItemManage.entity.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, good_id, good_name, price, quantity)
        VALUES (#{orderId}, #{goodId}, #{goodName}, #{price}, #{quantity})
    </insert>

    <!-- 根据订单ID删除明细 -->
    <delete id="deleteByOrderId">
        DELETE FROM order_item
        WHERE order_id = #{orderId}
    </delete>

</mapper>